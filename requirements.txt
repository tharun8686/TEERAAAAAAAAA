# Terra Edge - backend dependencies (all 7 FastAPI hazard microservices)
# Install with:  python -m pip install -r requirements.txt

# API layer
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# ML / data
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.2.0
joblib>=1.2.0
pyarrow>=12.0.0
pyyaml>=6.0

# Supabase backend (alerts, predictions, node inventory)
supabase>=2.0.0
python-dotenv>=1.0.0
